fn main() {
    //type data tuple
    //
    let angka = (1, 2, 3, 4, 5, 6);

    //karna type data angka integer,untuk mencetak harus
    //pakai {:?} untuk konver ke string

    println!("angka = {:?}", angka);
}
